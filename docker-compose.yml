# Generated by scripts/generate_compose.py — do not edit manually.
# To regenerate: python3 scripts/generate_compose.py

services:

  node-express:
    build: ./targets/node-express
    ports:
      - "3001:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s

  node-fastify:
    build: ./targets/node-fastify
    ports:
      - "3002:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s

  node-nestjs:
    build: ./targets/node-nestjs
    ports:
      - "3003:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s

  php-laravel:
    build: ./targets/php-laravel
    ports:
      - "3004:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s

  python-fastapi:
    build: ./targets/python-fastapi
    ports:
      - "3005:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s

  php-laravel-octane:
    build: ./targets/php-laravel-octane
    ports:
      - "3006:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s

  php-laravel-fpm:
    build: ./targets/php-laravel-fpm
    ports:
      - "3007:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s

  go-gin:
    build: ./targets/go-gin
    ports:
      - "3008:3000"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/holamundo"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 30s
